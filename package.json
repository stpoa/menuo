{
  "name": "menuo",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "run-p start:server start:client",
    "deploy": "run-p deploy:server deploy:client",
    "start:client": "cd ./client && yarn start",
    "deploy:client": "cd ./client && yarn deploy",
    "upload:client": "cd ./server && sls client deploy --no-config-change --no-policy-change --no-cors-change --no-confirm",
    "start:server": "cd ./server && yarn start",
    "deploy:server:dev": "cd ./server && yarn deploy:dev",
    "deploy:server:prod": "cd ./server && yarn deploy:prod",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {}
}
